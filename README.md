Sources for the plugin `plugin.appodeal.beta.Ogury`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.appodeal.beta.Ogury" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
